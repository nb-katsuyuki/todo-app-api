/**
 * to do sample project
 */

package model

// Todo一覧ページのviewvalue
case class TodoPage(
    title: String,
    cssSrc: Seq[String],
    jsSrc: Seq[String],
    todoList: Seq[TodoItem],
    debug: Any
) extends ViewValueCommon
